#ifndef HELPERS_HPP
#define HELPERS_HPP

#include <iostream>
#include <vector>
namespace Helpers {
std::pair<std::vector<std::vector<bool>>, std::vector<std::vector<bool>>>
GenerateMaze(const int dimension);
}  // namespace Helpers

#endif  // HELPERS_HPP
