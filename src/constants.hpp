#pragma once

#include <cmath>

namespace constants {
const float pi = 3.14f;
const float golden_ratio = (1 + std::sqrt(5.0f)) / 2;
const float wall_width = 2.9f;
const float wall_height = 2.9f;
const float wall_thickness = 0.1f;
const int screen_width = 700;
const int screen_height = 700;
const int maze_dimension = 5;
}  // namespace constants
